[package]
name = "structdb"
version = "0.1.0"
edition = "2021"
authors = ["David Kleingeld"]
description = "Build a database by defining a struct"

[features]

[dependencies]
structdb-derive = { path = "structdb-derive" }

serde = "1"
sled = "0.34"
