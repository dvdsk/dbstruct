[package]
name = "dbstruct"
version = "0.1.0"
edition = "2021"
authors = ["David Kleingeld"]
description = "Build a database by defining a struct"

[features]

[dependencies]
structdb-derive = { path = "dbstruct-derive" }
thiserror = "1"
sled = "0.34"
serde = { version = "1", features = ["derive"] }
bincode = "1.3"
